import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Home'
};

const Home = () => {
  return (
    <main>
      <h1>Home page</h1>
    </main>
  );
};

export default Home;
